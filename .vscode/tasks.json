{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "modern_cpp_constructors",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/modern_cpp/constructors/build"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": "$gcc"
        }
    ]
}
